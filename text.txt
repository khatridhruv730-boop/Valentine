<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>For Rashi Sood 💖</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      height: 100vh;
      overflow: hidden;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #ff4e8a, #ffb347);
      font-family: Arial, sans-serif;
      text-align: center;
    }

    h1, p {
      color: white;
      position: relative;
      z-index: 2;
    }

    h1 {
      font-size: 2.4rem;
      margin-bottom: 15px;
    }

    p {
      font-size: 1.25rem;
      margin-bottom: 25px;
    }

    .box {
      position: relative;
      width: 340px;
      height: 190px;
      z-index: 2;
    }

    button {
      padding: 12px 30px;
      font-size: 1.2rem;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      position: absolute;
    }

    #yes {
      background: #28a745;
      color: white;
      left: 50%;
      bottom: 10px;
      transform: translateX(-50%);
    }

    #no {
      background: #dc3545;
      color: white;
      top: 10px;
      left: 10px;
    }

    /* Floating hearts */
    .heart {
      position: absolute;
      bottom: -20px;
      font-size: 22px;
      animation: floatUp 6s linear infinite;
      opacity: 0.8;
    }

    @keyframes floatUp {
      from { transform: translateY(0); opacity: 1; }
      to   { transform: translateY(-110vh); opacity: 0; }
    }
  </style>
</head>

<body>

  <!-- Background Music -->
  <audio autoplay loop>
    <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
  </audio>

  <div>
    <h1>Rashi Sood 💖</h1>

    <p>
      Somewhere between smiles and moments,<br>
      you became my favorite thought of the day.
    </p>

    <p>
      So I made this… just to ask you properly 😌
    </p>

    <h1>Will you be my Valentine? 🌹</h1>

    <div class="box">
      <button id="yes" onclick="yesClicked()">Yes ❤️</button>
      <button id="no" onmouseover="moveNo()">No 🙈</button>
    </div>
  </div>

  <script>
    // NO button runs away
    function moveNo() {
      const noBtn = document.getElementById("no");
      const box = document.querySelector(".box");

      const maxX = box.clientWidth - noBtn.clientWidth;
      const maxY = box.clientHeight - noBtn.clientHeight;

      noBtn.style.left = Math.random() * maxX + "px";
      noBtn.style.top = Math.random() * maxY + "px";
    }

    // YES clicked
    function yesClicked() {
      document.body.innerHTML = `
        <div style="text-align:center;padding:20px;">
          <h1 style="color:white;">I knew you’d say YES 😌💘</h1>
          <p style="color:white;font-size:1.4rem;">
            Rashi, this just became my favorite screenshot ❤️<br><br>
            Thank you for making my Valentine special.
          </p>
          <p style="color:white;font-size:1.1rem;">
            (Now come say hi… I’m smiling already 😉)
          </p>
        </div>
      `;

      // Redirect to YOUR WhatsApp chat after 3 seconds
      setTimeout(() => {
        window.location.href =
          "https://wa.me/918699998668?text=I%20said%20YES%20to%20being%20your%20Valentine%20%F0%9F%92%96%20%E2%9D%A4%EF%B8%8F";
      }, 3000);
    }

    // Floating hearts generator
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = (20 + Math.random() * 20) + "px";
      heart.style.animationDuration = (4 + Math.random() * 3) + "s";
      document.body.appendChild(heart);

      setTimeout(() => heart.remove(), 7000);
    }, 350);
  </script>

</body>
</html>